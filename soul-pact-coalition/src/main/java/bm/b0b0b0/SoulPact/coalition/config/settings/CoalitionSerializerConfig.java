package bm.b0b0b0.SoulPact.coalition.config.settings;

import net.elytrium.serializer.SerializerConfig;

public final class CoalitionSerializerConfig {

    public static final SerializerConfig INSTANCE = new SerializerConfig.Builder().build();

    private CoalitionSerializerConfig() {
    }
}
