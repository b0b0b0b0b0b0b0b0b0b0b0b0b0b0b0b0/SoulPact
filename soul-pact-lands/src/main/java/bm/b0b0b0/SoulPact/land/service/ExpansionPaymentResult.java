package bm.b0b0b0.SoulPact.land.service;

public enum ExpansionPaymentResult {
    SUCCESS,
    TREASURY_INSUFFICIENT,
    LEADER_INSUFFICIENT,
    ECONOMY_UNAVAILABLE,
    TREASURY_LOCKED,
    FAILED
}
