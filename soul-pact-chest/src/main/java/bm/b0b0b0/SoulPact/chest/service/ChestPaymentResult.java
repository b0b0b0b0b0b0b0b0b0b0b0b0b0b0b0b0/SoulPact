package bm.b0b0b0.SoulPact.chest.service;

public enum ChestPaymentResult {
    SUCCESS,
    TREASURY_INSUFFICIENT,
    LEADER_INSUFFICIENT,
    TREASURY_LOCKED,
    ECONOMY_UNAVAILABLE,
    FAILED
}
